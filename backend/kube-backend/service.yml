apiVersion: v1
kind: Service
metadata:
  namespace: e-commerce
  name: backend-svc
spec:
  selector:
    app: backend
  ports:
    - name: backend-svc
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP